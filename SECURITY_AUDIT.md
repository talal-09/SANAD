# فحص أمان سَنَد

آخر فحص: 20 سبتمبر 2026.

## الحماية المطبقة

- جميع استعلامات البحث تستخدم Django ORM المعلّم بالمعاملات، ولا يوجد SQL خام أو `RawSQL` أو `extra()`.
- جميع نماذج POST تستخدم حماية CSRF، ولا توجد صفحات معفاة عبر `csrf_exempt`.
- قوالب Django تهرّب مدخلات المستخدم تلقائيًا، وتوجد سياسة CSP تمنع السكربتات والموارد غير المصرح بها.
- التحويل بعد تسجيل الدخول يقبل روابط داخل الموقع فقط.
- محاولات الدخول الفاشلة محدودة افتراضيًا بخمس محاولات لكل عنوان واسم مستخدم خلال عشر دقائق.
- التسجيل العام يتوقف تلقائيًا عندما تكون `DJANGO_DEBUG=0`؛ حسابات الإنتاج ينشئها المدير.
- بيانات المرضى والأشعة والمتابعات والتنبيهات مقيدة بمنشأة حساب المستخدم. محاولة فتح معرف تابع لمنشأة أخرى تعيد 404.
- ملفات DICOM والتقارير الطبية محفوظة خارج مجلد الوسائط العام.
- ملفات ZIP تخضع لحدود الحجم والعدد ونسبة الضغط، مع منع Zip Slip والروابط الرمزية والملفات التنفيذية.
- ملف التقرير يجب أن يحمل امتداد PDF وتوقيع PDF فعليًا، ويُنزل كمرفق خاص بعد التحقق من الصلاحية.
- صفحات المستخدم المسجل لا تُخزن في ذاكرة المتصفح أو الوكيل (`private, no-store`).
- فُعّلت رؤوس منع clickjacking وMIME sniffing وسياسة الصلاحيات وCSP.

## إعداد الإنتاج

اضبط القيم التالية في بيئة الخادم، ولا تحفظها داخل المستودع:

```text
DJANGO_DEBUG=0
DJANGO_SECRET_KEY=<قيمة عشوائية سرية بطول 50 محرفًا أو أكثر>
DJANGO_ALLOWED_HOSTS=sanad.example.sa
```

عند تعطيل وضع التطوير يفعّل سَنَد تلقائيًا HTTPS redirect وSecure cookies وHSTS. يجب تشغيله خلف خادم WSGI/ASGI إنتاجي مع شهادة HTTPS، وتحديد حد جسم الطلب في الخادم بما لا يتجاوز حد رفع DICOM المطلوب. لا تستخدم `manage.py runserver` للنشر.

نفّذ قبل أي نشر:

```powershell
python manage.py migrate
python manage.py test
python manage.py check --deploy
python -m pip check
```

## ملاحظات تشغيلية

- محدد محاولات الدخول الحالي يستخدم Django cache المحلي، وهو مناسب للنموذج الأولي وعملية خادم واحدة. عند تشغيل عدة عمليات أو خوادم، استخدم cache مركزيًا مثل Redis أو حد الطلبات في بوابة الويب.
- عزل المنشأة يعتمد على منشأة الموظف الذي سجل المريض. أي بيانات قديمة بلا `registered_by` لن تظهر للكادر حتى يربطها مدير النظام بمالك صحيح.
- يحتفظ النظام بنسخة Django المدعومة `6.1.1` ونسخة `pydicom 3.0.2` التي تتضمن إصلاح أمان DICOMDIR. راجع تحديثات التصحيح قبل كل نشر.
- هذا فحص للكود والإعدادات واختبارات داخلية، وليس اختبار اختراق خارجيًا للبنية السحابية أو الشبكة.
